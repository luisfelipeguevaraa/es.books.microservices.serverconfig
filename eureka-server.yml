server:
  port: 2222

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost/eureka/
    registerWithEureka: false
    fetchRegistry: false
  server:
    enable-self-preservation: false

